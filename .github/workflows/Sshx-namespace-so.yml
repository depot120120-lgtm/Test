name: SSHX Single Step

on:
  workflow_dispatch:

jobs:
  run-sshx:
    runs-on: namespace-profile-window # Only run on runner with this name
    steps:
      - name: Run SSHX
        run: curl -sSf https://sshx.io/get | sh -s run
